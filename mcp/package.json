{
  "name": "blind-bench-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Model Context Protocol server for Blind Bench. Wraps the /api/v1/* HTTP surface so agents (Claude Code, Cursor, custom runtimes) can author prompts, run comparisons, create review cycles, and submit blind evaluations.",
  "type": "module",
  "bin": {
    "blind-bench-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
