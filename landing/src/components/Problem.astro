<section class="redact-reveal border-y border-border/50 bg-muted/30 py-12 lg:py-16">
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:px-8">
    <h2 class="text-xl font-semibold tracking-tight text-foreground sm:text-2xl">
      Getting human feedback on prompts is broken.
    </h2>

    <div class="mt-6 max-w-[65ch] space-y-4 text-base leading-relaxed text-muted-foreground">
      <p>
        The only way to know your prompt is good is to put outputs in front of
        <span class="redacted">real humans</span>.
      </p>
      <p>
        Experts, teammates, customers — <span class="redacted redacted-delay">none of them want to log into another tool</span>.
      </p>
      <p class="font-medium text-foreground">
        Blind Bench: one link to collect their feedback. One click to turn it
        into a better prompt.
      </p>
    </div>
  </div>
</section>

<style>
  .redacted {
    display: inline;
    filter: blur(4px);
    transition: filter 0.5s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .redacted-delay {
    transition-delay: 0.3s;
  }

  .redact-reveal.is-visible .redacted {
    filter: blur(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .redacted {
      filter: none;
      transition: none;
    }
  }
</style>
